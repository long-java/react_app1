import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#c0c0c0",
            light: "ffff00"
        },
        secondary: {
            main: "#ff0000"
        },
        otherColor: {
            main: "#0000ff"
        }
    }
})